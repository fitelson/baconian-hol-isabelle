% Algebraic bridge: Y-relative transport implies weak L2.

include('00_common_semantics.tff.in').

tff(assume_y_transport,axiom,
    ! [W:world] : y_transport(W)).

tff(y_transport_implies_weak_L2,conjecture,
    ! [W:world] : weak_L2(W)).
