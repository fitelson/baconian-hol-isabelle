# Staged TFF results, 29 July 2026

Vampire 5.0.1 parsed and clausified the shared base and all three target files.
Each proof search used six cores and a 60-second limit.

| File | SZS status | Limit |
|---|---:|---:|
| `04b_full_T0_PP_implies_classification.tff.in` | Timeout | 60 s, 6 cores |
| `06b_full_T0_PP_implies_orbit.tff.in` | Timeout | 60 s, 6 cores |
| `08b_full_T0_PP_implies_y_transport.tff.in` | Timeout | 60 s, 6 cores |

No conjecture was admitted as an axiom, and the common base imports neither
`not_L2` nor the old `$false` target.

These timeouts establish neither nonderivability nor consistency.  They show
that the obstruction found by the staged THF search persists after operators
are reified into the first-order sort `op1`: the represented
\(T_0+\mathrm{PP}+\exists\fun'\) background does not quickly yield composition
classification, full `fun'`-orbit reachability, or the weaker \(Y\)-relative
transport principle.

The TFF result is weaker evidence than the THF result.  In particular, a proof
would still require Isabelle replay, while a model or timeout would not refute
derivability in the genuine higher-order theory.
